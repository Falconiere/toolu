# Set up comemory

Read `${CLAUDE_PLUGIN_ROOT}/workflows/setup.md` completely, then execute the
canonical workflow with `TOOLU_HOST_OVERRIDE=claude` in the setup script's
environment. Pass `$ARGUMENTS` to the setup script only as described by that
workflow.
