name: Secret Scan

on:
  pull_request:
  push:
    branches: [main]
  schedule:
    - cron: "0 4 * * *"

permissions:
  contents: read
  pull-requests: read
  security-events: write
  issues: write

jobs:
  scan:
    uses: Falconiere/workflows/.github/workflows/secret-scan.yml@v2
