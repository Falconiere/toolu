# CLAUDE.md

@AGENTS.md

<!-- A pointer, not a copy. AGENTS.md is the single source of truth every agent
     reads; the import above loads it at launch. Add instructions there — a
     second copy here would drift, invisibly, because each agent reads only
     its own file. -->
